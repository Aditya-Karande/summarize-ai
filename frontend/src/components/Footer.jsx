// ─────────────────────────────────────────────
//  COMPONENT — Footer
// ─────────────────────────────────────────────

import styles from "../styles/styles";

export default function Footer() {
  return (
    <footer style={styles.footer}>
      Paste any text — articles, papers, reports — and get the key points instantly.
    </footer>
  );
}
